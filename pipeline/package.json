{
  "name": "zgm-gliwice-pipeline",
  "version": "0.1.0",
  "description": "Scrapes ZGM Gliwice auction results, OCRs PDFs, builds data/*.json for the Chrome extension",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "refresh": "node src/refresh.js",
    "crawl:results": "node src/crawl-results.js",
    "crawl:active": "node src/crawl-active.js",
    "test": "node --test --test-reporter=tap tests/ 2>&1 | grep -E '^not ok|^  ---|^  ...$|# fail|# pass' || true"
  }
}

node_modules/
pipeline/downloads/
pipeline/pages-cache/
*.log
.DS_Store
.vscode/
.idea/
_smoke.mjs
_*.mjs
extension.zip
# Build artifact: build-site.sh regenerates _site/ (rm -rf + rebuild) and the
# Cloudflare/OVH/Pages deploys all build it fresh. The committed copy went
# stale and shadowed the real source during debugging — never track it.
_site/

# Packaged extension upload bundles (rebuilt from extension/ per release).
przetargimiejskie-extension-*.zip
# B2G outreach artifacts are regenerated from committed data. The one verified
# public example is tracked under site/dla-samorzadow/przyklad-gliwice/ instead.
outreach/*/one-pager-*.html
outreach/*/karta-wynikow-*.html
outreach/*/karta-wynikow-*.pdf
outreach/*/karta-wynikow-*.csv
